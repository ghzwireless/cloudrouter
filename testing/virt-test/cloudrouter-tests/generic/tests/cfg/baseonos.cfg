- baseonos: install setup image_copy unattended_install.cdrom
    virt_test_type = qemu libvirt
    type = baseonos
    kill_vm_on_error = no
    login_timeout = 240
    nettype = bridge
    netdst = virbr0

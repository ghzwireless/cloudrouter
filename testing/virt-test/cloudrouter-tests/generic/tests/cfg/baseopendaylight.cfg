- baseopendaylight: install setup image_copy unattended_install.cdrom
    virt_test_type = qemu libvirt
    type = baseopendaylight
    kill_vm_on_error = no
    login_timeout = 240
    nettype = bridge
    netdst = virbr0
